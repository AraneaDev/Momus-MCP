import{_ as o,o as t,c as s,a0 as i}from"./chunks/framework.DQOulFGV.js";const u=JSON.parse('{"title":"7. Phased Implementation Roadmap","description":"","frontmatter":{},"headers":[],"relativePath":"07-roadmap.md","filePath":"07-roadmap.md"}'),a={name:"07-roadmap.md"};function n(r,e,c,d,l,p){return t(),s("div",null,[...e[0]||(e[0]=[i('<h1 id="_7-phased-implementation-roadmap" tabindex="-1">7. Phased Implementation Roadmap <a class="header-anchor" href="#_7-phased-implementation-roadmap" aria-label="Permalink to &quot;7. Phased Implementation Roadmap&quot;">​</a></h1><blockquote><p>Normative. The build order for Momus-MCP. Each phase lists goals, deliverables, acceptance criteria, exit criteria, and top risks. Sizes are relative (S/M/L) for scheduling.</p></blockquote><h2 id="phase-0-—-scaffold-s-1-week-·-✅-done-see-10-build-plan-md" tabindex="-1">Phase 0 — Scaffold (S, ~1 week) · ✅ DONE (see <code>10-build-plan.md</code>) <a class="header-anchor" href="#phase-0-—-scaffold-s-1-week-·-✅-done-see-10-build-plan-md" aria-label="Permalink to &quot;Phase 0 — Scaffold (S, ~1 week) · ✅ DONE (see `10-build-plan.md`)&quot;">​</a></h2><blockquote><p>Implemented with <strong>npm workspaces</strong> (pnpm was unconfirmed in the build environment and the spec now mandates npm — drop-in swap if a project prefers pnpm).</p></blockquote><p><strong>Goals:</strong> repository skeleton per §6.2; CI green; the spec itself is the first artifact.</p><p><strong>Deliverables:</strong></p><ul><li>npm workspaces monorepo with <code>core</code>, <code>parser-typescript</code>, <code>server</code>, <code>cli</code> packages (implemented, running from <code>src/</code> via <code>tsx</code> — no <code>dist/</code> build in Phase 1).</li><li><code>ci.yml</code> unit job + lint + typecheck running.</li><li><code>schemas/momusrc.schema.json</code> + <code>.momusrc</code> parser with <code>SYS-005</code> diagnostics.</li><li><code>docs/</code> (this specification) committed; <code>docs/README.md</code> is the entry point.</li></ul><p><strong>Acceptance:</strong></p><ul><li><code>pnpm build &amp;&amp; pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm lint</code> pass on a clean checkout.</li><li><code>.momusrc</code> schema validates; invalid config produces <code>SYS-005</code> with a precise message.</li></ul><p><strong>Exit:</strong> repo builds in CI; skeleton merged.</p><hr><h2 id="phase-1-—-minimal-viable-daemon-ts-vitest-engine-l-4–6-weeks-·-✅-core-done-see-10-build-plan-md" tabindex="-1">Phase 1 — Minimal Viable Daemon + TS/Vitest Engine (L, ~4–6 weeks) · ✅ CORE DONE (see <code>10-build-plan.md</code>) <a class="header-anchor" href="#phase-1-—-minimal-viable-daemon-ts-vitest-engine-l-4–6-weeks-·-✅-core-done-see-10-build-plan-md" aria-label="Permalink to &quot;Phase 1 — Minimal Viable Daemon + TS/Vitest Engine (L, ~4–6 weeks) · ✅ CORE DONE (see `10-build-plan.md`)&quot;">​</a></h2><blockquote><p>Engine, parser, MCP server, CLI, and test suites are <strong>implemented and green</strong> (130 tests). All Phase-1 items complete: v0.1 packaging (Step 1 of <code>10-build-plan.md</code>), <code>better-sqlite3</code> IR cache, <code>chokidar</code> watcher, eslint/prettier, coverage gate.</p></blockquote><p><strong>Goal:</strong> <code>momus serve</code> + CLI running the full Phase-1 rule set on TypeScript/Vitest/Jest with deterministic, token-budgeted output.</p><p><strong>Deliverables:</strong></p><ol><li><strong>Parser (<code>parser-typescript</code>)</strong> — TS compiler API → IR (§2.3): symbols, imports, mocks (catalog §2.5.1), assertions, dataflow (§3.2). Type-aware mode via incremental <code>ts.Program</code> <strong>built over a custom host with parent-enabled source files</strong> (§2.2.2 constraints F5/F6 — non-negotiable: default program files have no <code>node.parent</code>, and checker queries need the program&#39;s own file instances) when <code>tsconfig.json</code> exists; syntax-only fallback with <code>SYS-003</code> notes.</li><li><strong>Engine (<code>core</code>)</strong> — <code>SymbolIndex</code> with resolution (§2.4), cache via <code>better-sqlite3</code> (§2.4.3), rule engine (§3.1), rules <strong>TAUT-001…006, DRIFT-000…005, MOCK-001, MOCK-002</strong> (TS), assignability (§3.4), suppression (§3.5).</li><li><strong>Formatters</strong> — Markdown + JSON envelope (§5), token budgets enforced + asserted.</li><li><strong>MCP server (<code>server</code>)</strong> — <code>audit_test_fidelity</code>, <code>detect_tautological_assertions</code>, <code>verify_mock_drift</code> (workspace scope only), <code>synthesize_mock_contract</code>, <code>list_rules</code>; capabilities + annotations per §4.1/§4.2; <code>chokidar</code> watcher keeps the index warm.</li><li><strong>CLI (<code>cli</code>)</strong> — <code>momus serve</code>, <code>momus audit &lt;paths&gt;</code>, <code>momus contract &lt;target&gt;</code>, <code>momus rules</code>, <code>momus init</code>, <code>momus doctor</code>; exit codes (0 clean / 1 findings / 2 config / 3 internal).</li><li><strong>Test suites</strong> — unit + gallery fixtures for every Phase-1 rule + clean corpus + golden outputs + MCP session tests (§6.6).</li></ol><p><strong>Acceptance criteria:</strong></p><ul><li>Every §3.3.1/§3.3.2/§3.3.3 Phase-1 rule has a gallery fixture with pinned golden output, and the clean corpus yields <strong>zero</strong> findings.</li><li><code>audit_test_fidelity</code> single-file &lt; 200 ms; index 10k LOC cold &lt; 1 s (§2.7).</li><li><code>tools/list</code> matches §4.2 exactly; all tools are read-only per annotations.</li><li>The false-green scenarios from §1.2.1 (over-mock, tautology, drift, zero-reach) are each detected end-to-end through the MCP surface.</li><li>Self-audit: <code>momus audit .</code> on the Momus repo reports 0 issues.</li></ul><p><strong>Exit criteria:</strong> a coding agent can be pointed at the Phase-1 server and reliably blocked from declaring &quot;done&quot; on a false-green TS suite. <strong>Top risks:</strong> dataflow precision on <code>beforeEach</code>-configured mocks (mitigate: conservative scope linking, §3.7); TS program memory on huge repos (mitigate: incremental program + file caps).</p><hr><h2 id="phase-2-—-php-support-multi-language-normalizer-l-4-weeks-·-✅-complete-see-10-build-plan-md-step-3" tabindex="-1">Phase 2 — PHP Support &amp; Multi-language Normalizer (L, ~4 weeks) · ✅ COMPLETE (see <code>10-build-plan.md</code> Step 3) <a class="header-anchor" href="#phase-2-—-php-support-multi-language-normalizer-l-4-weeks-·-✅-complete-see-10-build-plan-md-step-3" aria-label="Permalink to &quot;Phase 2 — PHP Support &amp; Multi-language Normalizer (L, ~4 weeks) · ✅ COMPLETE (see `10-build-plan.md` Step 3)&quot;">​</a></h2><p><strong>Goal:</strong> parity for PHP (PHPUnit/Pest) with the same rules, one shared engine.</p><p><strong>Deliverables:</strong></p><ul><li><code>parser-php</code> implementing <code>LanguageParser</code> via <code>php-parser</code> (§2.2.3): namespaces/<code>use</code> resolution to FQCN, constructor promotion, ✅ docblock <code>@return</code>/<code>@param</code> typing, ✅ anonymous classes, ✅ <code>getMockForAbstractClass</code> as a distinct pattern, ✅ setUp/property mock bindings (<code>$this-&gt;prop = createMock(...)</code>), Mockery/Pest patterns (§2.5.2).</li><li>PHP galleries + clean corpus + goldens for all rules; DRIFT-004 constructor-awareness; PHPUnit constructor-bypass exemption (§3.5.3).</li><li>✅ <code>synthesize_mock_contract</code> gains <code>phpunit</code>/<code>pest</code> templates.</li><li>✅ <code>momus doctor</code> reports PHP-language readiness; <code>languages.php.enabled</code> in <code>.momusrc</code>.</li></ul><p><strong>Acceptance criteria:</strong></p><ul><li>Rule parity matrix (rule × framework) fully green in CI: <code>TAUT-*</code>, <code>DRIFT-*</code>, <code>MOCK-*</code> behave identically across vitest/jest/phpunit/pest fixtures (outputs differ only in framework-appropriate code spans).</li><li>PHP fixtures parse with zero <code>SYS-001</code>; clean PHP corpus: zero findings.</li></ul><p><strong>Exit:</strong> both language families audited by one binary with identical rule semantics. <strong>Top risks:</strong> <code>php-parser</code> typing fidelity (mitigate: docblock parsing + documented nikic sidecar upgrade path); Mockery closure-form mocks (<code>Mockery::mock(&#39;Foo&#39;, fn($m) =&gt; …)</code>) — handle via closure-body member extraction.</p><hr><h2 id="phase-3-—-git-diff-awareness-hooks-interactive-cli-m–l-3-weeks-·-✅-complete" tabindex="-1">Phase 3 — Git-Diff Awareness, Hooks &amp; Interactive CLI (M–L, ~3 weeks) · ✅ COMPLETE <a class="header-anchor" href="#phase-3-—-git-diff-awareness-hooks-interactive-cli-m–l-3-weeks-·-✅-complete" aria-label="Permalink to &quot;Phase 3 — Git-Diff Awareness, Hooks &amp; Interactive CLI (M–L, ~3 weeks) · ✅ COMPLETE&quot;">​</a></h2><p><strong>Goal:</strong> Momus in the developer/agent loop <em>before</em> commit, not just on demand.</p><p><strong>Deliverables (implemented):</strong></p><ul><li>✅ <strong>Diff scoping:</strong> <code>gitChangedPaths</code> (<code>--name-status</code> + <code>--find-renames</code>, untracked files and rename pairs) → <code>DiffScope</code> feeding DRIFT-006 (stale-mock) and diff-filtered drift rules. Wired through <code>momus audit|drift --git-diff --base REF</code>, <code>momus precommit</code> (default base HEAD), and MCP <code>verify_mock_drift</code> with <code>scope: git-diff</code> + <code>baseRef</code> (git errors surface as tool errors).</li><li>✅ <strong>Pre-commit companion:</strong> <code>momus precommit</code> — drift-only run vs the diff; exits 1 on error findings. <code>momus hook</code> git-hook installer is also implemented: <code>hook --install</code>/ <code>--uninstall</code> (marker-guarded, <code>--yes</code>-gated) and <code>hook</code> runs the staged-files drift gate. (Staged-line granularity remains a later sub-slice.)</li><li>✅ <strong>annotate-pr:</strong> <code>momus annotate-pr</code> posts GitHub Checks API annotations from the diff-scoped audit (dependency-free; reads <code>GITHUB_TOKEN</code>/<code>GITHUB_REPOSITORY</code>/<code>GITHUB_SHA</code>, <code>MOMUS_FAIL_ON</code> policy).</li><li>✅ <strong>Streamable HTTP transport:</strong> <code>momus serve --transport http [--port N]</code> serves the same five tools over Streamable HTTP (stateful per-session transports, stateless tools); covered by an end-to-end <code>StreamableHTTPClientTransport</code> round-trip.</li><li>✅ <strong>JSONL annotate mode:</strong> <code>momus annotate [paths...] [--git-diff --base REF]</code> emits one JSON object per finding (workspace-relative file/line/column, rule/severity/message, deterministic key order) for editor plugins; exits 1 on error findings.</li><li>✅ <strong><code>--fix</code> mechanism:</strong> <code>momus audit --fix</code> (dry-run unified diff by default) with <code>--yes</code> to apply and a CI-refusal gate (§1.5); <code>collectFixable</code>/<code>applyFixes</code>/<code>unifiedDiff</code> unit-tested. DRIFT-001 emits a real rename fix (unique near-match, quoted per stub api); a planted stale spy is diffed, applied, and re-audits clean. TAUT-001/002/003 are semantic tautologies — no safe mechanical fix exists — so they stay descriptive-only (a documented decision, §3.6).</li><li>✅ <strong>File watching:</strong> <code>momus serve --watch</code> (chokidar) invalidates the <code>ts.Program</code> cache on source add/change/unlink, so watch-mode audits reflect on-disk edits without a restart.</li></ul><p><strong>Acceptance criteria:</strong></p><ul><li>Hook on a fixture repo with a planted drift violation: commit blocked; <code>git diff</code>-scoped run completes &lt; 500 ms on 10k-LOC workspace.</li><li><code>--fix</code> on the planted stale-spy fixture produces a correct, minimal rename diff; dry-run output is byte-identical to <code>--fix --dry-run</code> preview. (TAUT-* findings are semantic and intentionally not auto-fixed — see §3.6.)</li><li><code>DRIFT-006</code> fires only when target changed and mock file untouched (no false positives on untouched-but-stale mocks outside the diff).</li></ul><p><strong>Exit:</strong> &quot;commit-time&quot; and &quot;agent-loop&quot; paths both enforced; findings converge to zero before merge. <strong>Top risks:</strong> git plumbing edge cases (renames, merge commits) — mitigate with <code>git diff --name-status</code> + <code>--find-renames</code>, fallback to workspace scope with <code>SYS-003</code> note.</p><hr><h2 id="phase-4-—-ci-cd-action-public-distribution-m-2–3-weeks-·-in-progress" tabindex="-1">Phase 4 — CI/CD Action &amp; Public Distribution (M, ~2–3 weeks) · IN PROGRESS <a class="header-anchor" href="#phase-4-—-ci-cd-action-public-distribution-m-2–3-weeks-·-in-progress" aria-label="Permalink to &quot;Phase 4 — CI/CD Action &amp; Public Distribution (M, ~2–3 weeks) · IN PROGRESS&quot;">​</a></h2><p><strong>Goal:</strong> Momus as a standard part of the pipeline and discoverable by any agent.</p><p><strong>Deliverables:</strong></p><ul><li>✅ <strong>GitHub Action</strong> (<code>packages/action/action.yml</code>, spec §6.5.3): composite action with diff-scoped audit + <code>annotate-pr</code> check annotations, <code>fail-on</code> input, base defaulting to the PR base SHA. (Not yet end-to-end CI-tested via <code>act</code>; publish + registry listing blocked on credentials.)</li><li>✅ <strong>Release scaffolding:</strong> <code>release-please</code> (Knossos-style single lockstep version from <strong>0.0.1</strong>; <code>release-please-config.json</code> + <code>.release-please-manifest.json</code>, <code>json</code> extra-files bumping all five workspace <code>package.json</code>s), <code>pr-title.yml</code> conventional-commit gate, <code>.github/workflows/release-please.yml</code> (version-PR → <code>v*</code> tag + GitHub Release → <code>npm run publish</code> in dependency order). Actual publish is <strong>manual-only</strong> (not in CI) until sanctioned.</li><li>Registry publishing: npm packages (<code>@momus/*</code>), MCP registry listings (official MCP servers list + community registries) with <code>npx -y @momus/mcp-server</code> install snippet; README quickstart for Claude Desktop / other clients. — pending credentials</li><li><strong>Docs site</strong> (docs/ rendered) + changelog-driven releases (§6.7). — changelog scaffolding shipped via release-please; docs-site rendering pending</li><li><strong>Soak corpus CI</strong> (optional but enabled): OSS corpus precision/perf triage (§6.6.5).</li></ul><p><strong>Acceptance criteria:</strong></p><ul><li>Action runs on a demo PR: annotations posted, check fails on error findings, passes after fixes (end-to-end test in CI against a fixture repo via <code>act</code> or a GitHub-hosted fixture).</li><li><code>npm view @momus/cli</code> resolves; <code>npx -y @momus/cli audit .</code> works in a clean container.</li><li>Registry listings link to working install commands; server passes a smoke <code>tools/list</code> + <code>tools/call</code> against the published package.</li></ul><p><strong>Exit:</strong> a repository can adopt Momus in under five minutes (action or CLI), and any MCP client can attach <code>momus-mcp</code> the same way it attaches any other server.</p><hr><h2 id="cross-cutting-concerns-all-phases" tabindex="-1">Cross-cutting concerns (all phases) <a class="header-anchor" href="#cross-cutting-concerns-all-phases" aria-label="Permalink to &quot;Cross-cutting concerns (all phases)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Concern</th><th>Requirement</th></tr></thead><tbody><tr><td>Determinism</td><td>Byte-identical output for identical workspace state; golden-tested (§5.5).</td></tr><tr><td>Token budgets</td><td>Enforced from Phase 1; authoring lints in CI (§5.1).</td></tr><tr><td>Security</td><td>Threat model §1.5 holds in every phase; the <code>--fix</code>/hook writes are the only writes and are gated.</td></tr><tr><td>Dogfooding</td><td>Self-audit CI job from Phase 1 onward (P9).</td></tr><tr><td>Docs</td><td>Spec updates ship with the code change that contradicts them; <code>docs/README.md</code> index always current.</td></tr></tbody></table><h2 id="sequencing-notes" tabindex="-1">Sequencing notes <a class="header-anchor" href="#sequencing-notes" aria-label="Permalink to &quot;Sequencing notes&quot;">​</a></h2><ul><li>Phase 1 is the critical path and is self-contained: the MCP server alone already delivers the core value (&quot;agents can&#39;t claim false-green anymore&quot;).</li><li>Phase 2 can proceed in parallel with Phase 3 only if the <code>core</code> rule surface is frozen — the rule interface (§3.1) is the freeze boundary; treat any change to <code>RuleContext</code> as a breaking change (major version bump).</li><li>Phase 4 depends on all earlier phases&#39; acceptance criteria; registry listings must not ship before <code>tools/list</code> output is stable (prompt-cache stability requirement, §4.1).</li></ul>',48)])])}const g=o(a,[["render",n]]);export{u as __pageData,g as default};
